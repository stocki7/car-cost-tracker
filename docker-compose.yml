services:
  app:
    build: ./backend
    restart: unless-stopped
    volumes:
      - ./data:/data
      - ./frontend:/frontend:ro
    ports:
      - "8081:8000"
    environment:
      - TZ=Europe/Vienna
